import { describe, it } from "vitest";

describe("question/[id] page", () => {
  describe("if the test is started", () => {
    it("renders the current question", () => {});
    it("renders a timer", () => {});
    it("the possible answers", () => {});
    it("the users progress", () => {});
  });

  describe("if the test has not been started", () => {
    it("navigates to /", () => {});
  });
});
